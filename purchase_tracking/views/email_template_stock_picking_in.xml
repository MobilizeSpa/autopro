<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!--Email template -->
        <record id="email_template_stock_picking_in" model="mail.template">
            <field name="name">Recepción de tracking - enviar por email</field>
            <field name="email_from">${(object.create_uid.email and '%s &lt;%s&gt;' % (object.create_uid.name, object.create_uid.email) or '')|safe}</field>
            <field name="subject">${object.company_id.name} - Recepción (Ref ${object.name or 'n/a' })</field>
            <field name="partner_to">${object.partner_id.id}, ${object.create_uid.id}</field>
            <field name="model_id" ref="purchase_tracking.model_stock_picking"/>
            <field name="auto_delete" eval="True"/>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="body_html"><![CDATA[
<p>Hola
% if object.partner_id.is_company and object.partner_id.child_ids:
    ${object.partner_id.child_ids[0].name}
% else :
    ${object.partner_id.name}
% endif
,</p>

<p>
Se confirma la recepción de sus productos en el documento, <strong>${object.name}</strong>.
</p>

<p>Saludos,</p>
]]></field>
        </record>
    </data>
</odoo>